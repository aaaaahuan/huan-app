# AGENTS.md

## 1. 适用范围与协作原则

- 本文件适用于整个 huan-app 仓库，作为编码代理与开发者的协作约定。
- 默认使用中文沟通。先阅读相关代码和文档，再提出结论或实施修改；不把猜测当作事实。
- 需求存在歧义、设计与实现冲突时，说明差异并确认，不擅自扩大范围。
- 优先实现当前需求所需的最小方案，如无必要，勿增实体。
- 保留用户已有改动，只修改本次任务相关内容；不得擅自回退、覆盖或整理无关文件。

## 2. 环境与开发命令

运行时与包管理器版本以 `.nvmrc`、`package.json` 为准。使用 pnpm，保持 `pnpm-lock.yaml`，不混入其他包管理器的锁文件。

| 命令 | 用途 |
| --- | --- |
| `nvm use` | 切换项目 Node 版本 |
| `pnpm install --frozen-lockfile` | 按锁文件安装依赖 |
| `pnpm dev` | 启动开发服务器和 Electron |
| `pnpm check` | 类型检查、Lint、生产构建 |
| `pnpm build` | 生成 `out/`，不生成应用包 |
| `pnpm pack:arm64` | 打包本机 Apple Silicon 应用 |
| `pnpm pack:x64` | 打包 Intel 应用，仍需对应环境验收 |

开发 UI 通过本地 Vite 服务加载，生产 UI 通过 `app://ui/index.html` 加载。不得将特权 UI 指向远程网站。不要随任务顺手升级依赖；新增依赖需说明必要性。

## 3. 代码与接口规范

- 使用 TypeScript，沿用现有格式和命名；避免 `any`、无依据的类型断言和屏蔽检查。
- 对复杂流程、安全约束和非直观设计补充简洁中文注释，解释原因，不逐行复述代码。
- 跨模块导入优先使用 `@shared/*`、`@main/*`、`@renderer/*`；同目录可用相对路径。新增别名需同时维护 TypeScript 与 electron-vite 配置。
- IPC 通道使用集中定义的常量，契约放在对应的 shared 文件中，处理逻辑保留在功能模块。
- 新增跨进程能力需同步维护契约、主进程校验和 preload；`window.huanApp` 类型统一维护，不在组件中重复声明。
- 文件读写、Electron 能力放在主进程；React 负责界面、交互和展示状态。
- 显式处理异步失败、取消及过期结果，避免旧请求覆盖新选择；不要吞掉需要向用户反馈的错误。
- 配置与副本保存沿用校验和原子替换机制，失败不得破坏最近有效数据。

## 4. UI 与交互规范

- 延续现有简洁、淡色系视觉，统一输入框、下拉框、按钮、间距与状态提示；不擅自更换已确认的图标。
- 保证键盘操作、焦点、可访问名称及加载、空、失败状态可用。
- 多用组件抽象，避免重复实现相同功能，针对图标、按钮、输入框、下拉框等常用组件，优先将其抽象到 `@renderer/components/` 目录下。避免在代码中出现繁荣的html标签组件组合以及svg相关代码，保持组件的可维护性和可扩展性。
- 抽象组件应保持状态无状态，不依赖外部状态或全局变量

## 5. 验证与交付

- 当前不引入单元测试、E2E、测试依赖或测试脚本，每一轮变更后自行判断如果有较复杂的逻辑变化，需要直接启动项目利用computer-use等工具进行自主验证-发现问题-修复问题-再次验收的循环，直到所有功能验证通过。

## 6. Git 与文档维护

- 修改前后检查工作区差异；不提交缓存、构建产物、本机配置或敏感信息。
- 仅在用户要求时提交或推送，提交前核对暂存范围，不擅自 amend 或执行破坏性 Git 操作。
- 提交说明概括实际改动，可沿用 `feat:`、`fix:`、`docs:` 等前缀。
- 行为、命令、接口或数据格式发生变化时，同步维护相关文档；不在多处复制大量实现细节。
