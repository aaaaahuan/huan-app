# A5 / A6 基础阅读交付

## 实现范围

- A5：分平台内存/持久会话，兼容旧配置，保存后完整重启生效；原站登录；清除内存和持久分区需要确认，不修改收藏。
- A6：平台筛选统一浅绿色视觉并保留原生菜单，搜索图标、设置双分类、键盘切换与阅读空图标；全量列表使用浏览器离屏绘制优化，不分页。
- 仍不引入测试框架、测试脚本或 AI 功能。

## 检查记录

代码基线已提交 `a9649e3`。类型检查、Lint 和生产构建通过；独立代码审查未发现新增阻塞问题。通过电脑操作按完整路径打开 `release/mac-arm64/huan-app.app`，确认显示“阅读空间”和设置中的“基础版 A6”。桌面旧应用此前仍是 A2/A3，不能据此判断新代码效果。

| 场景 | 当前结果 |
| --- | --- |
| 启动读取现有 v1 配置与笔记 | 已验证，18 条，未配置会话模式时默认内存 |
| 平台筛选、无匹配空图标 | 已验证 X / Reddit 切换，Reddit 空列表仅图标 |
| X 原始网页 | 已验证帖子正文和原站登录入口显示 |
| 设置覆盖原页 | 已验证，设置打开后网页隐藏，无原生视图盖住弹窗 |
| 设置来源 / 登录与隐私 | 已验证切换和三平台当前策略显示 |
| 清除前确认 | 已验证原生确认框，实际清除需授权后验收 |
| 原页内跳转、后退 / 前进 | 已验证 X 帖子跳转作者页，收藏选择不变，可返回和前进 |
| 切换收藏再返回 | 已验证回到原始帖子地址，不保留上一帖浏览历史 |
| 侧栏收起 / 展开 | 已验证网页区域重新布局，无遮挡 |
| 搜索无匹配 | 已验证空列表图标，当前阅读页面保持不变 |
| YouTube 原页 | 已显示播放器并播放，验收后已暂停；平台仍可能要求登录或验证 |
| 无效来源路径 | 已验证保存被拦截并提示；已放弃临时草稿，未修改原配置 |
| 设置分类键盘操作 | 已验证方向键切换并同步焦点 |
| 桌面应用更新 | 已重新打包并替换桌面 huan-app.app，按完整路径打开确认 A6 版本 |

## 验收边界

真实账号密码、验证码和平台授权由用户操作；未完成真实登录前，不声称登录跨重启已验证。下载、非网页协议、独立 POST 弹窗、DRM 和第三方 OAuth 限制不做绕过。Intel 真机、签名及公证另行验证，本次为本机 ARM64 开发交付，不用于公开分发。

实际清除、保存策略切换及真实登录跨重启待用户授权后验收；最小窗口尺寸尚未实测。上述边界不计为已通过项。
